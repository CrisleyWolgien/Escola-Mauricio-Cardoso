import { useEffect, useState } from "react";
import { Link, Navigate } from "react-router-dom";
import AdminImageUpload from "../components/AdminImageUpload";
const API_URL = import.meta.env.VITE_API_URL || "http://127.0.0.1:8001/api";
const empty = { title: "", summary: "", content: "", category: "Projetos", status: "draft" };
export default function AdminPosts() { const token = sessionStorage.getItem("emef_admin_token"); const [posts, setPosts] = useState([]); const [form, setForm] = useState(empty); const [selected, setSelected] = useState(null); const [image, setImage] = useState(""); if (!token) return <Navigate to="/admin" replace />; const headers={"Content-Type":"application/json",Authorization:`Bearer ${token}`}; const load=()=>fetch(`${API_URL}/posts/admin`,{headers}).then(r=>r.ok?r.json():[]).then(setPosts); useEffect(()=>{load();},[]); async function save(e){e.preventDefault();const r=await fetch(`${API_URL}/posts/admin`,{method:"POST",headers,body:JSON.stringify(form)});if(r.ok){const p=await r.json();setSelected(p);setForm(empty);load();}} async function addImage(e){e.preventDefault();if(!selected||!image)return;await fetch(`${API_URL}/posts/admin/${selected.id}/images`,{method:"POST",headers,body:JSON.stringify({image_url:image,is_published:true,display_order:selected.images?.length||0})});setImage("");load();} return <main className="min-h-screen bg-[#fbf7ee] p-5"><div className="mx-auto max-w-6xl"><Link to="/admin/painel" className="font-bold text-violet-700">← Painel</Link><h1 className="mt-5 font-RobotoSlab text-4xl font-black">Projetos e posts</h1><div className="mt-7 grid gap-8 lg:grid-cols-2"><form onSubmit={save} className="space-y-4 rounded-3xl bg-white p-6 shadow"><input required placeholder="Título" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="w-full rounded-xl border p-3"/><textarea required placeholder="Resumo" value={form.summary} onChange={e=>setForm({...form,summary:e.target.value})} className="w-full rounded-xl border p-3"/><textarea required placeholder="Texto completo" rows="7" value={form.content} onChange={e=>setForm({...form,content:e.target.value})} className="w-full rounded-xl border p-3"/><input value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="w-full rounded-xl border p-3"/><label><input type="checkbox" checked={form.status==="published"} onChange={e=>setForm({...form,status:e.target.checked?"published":"draft"})}/> Publicar</label><button className="block w-full rounded-xl bg-violet-700 p-3 font-black text-white">Criar post</button></form><div><h2 className="font-RobotoSlab text-2xl font-black">Posts criados</h2>{posts.map(p=><button key={p.id} onClick={()=>setSelected(p)} className="mt-3 block w-full rounded-2xl bg-white p-4 text-left shadow"><b>{p.title}</b><br/><small>{p.status==="published"?"Publicado":"Rascunho"}</small></button>)}</div></div>{selected&&<form onSubmit={addImage} className="mt-8 rounded-3xl bg-violet-50 p-6"><h2 className="font-RobotoSlab text-2xl font-black">Fotos: {selected.title}</h2><AdminImageUpload token={token} value={image} onChange={setImage} label="Adicionar foto"/><button className="mt-4 rounded-xl bg-violet-700 px-5 py-3 font-black text-white">Adicionar à galeria do post</button></form>}</div></main>; }
